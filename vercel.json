{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@astrojs/vercel"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.ts" },
    { "handle": "filesystem" }
  ]
}
